const initialState = {}

export const loginReducer = (state: any = initialState, action: any) => {
    switch (action.type) {
        case '1':
            return state
        default:
            return state
    }
}