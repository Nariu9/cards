const initialState = {}

export const registerReducer = (state: any = initialState, action: any) => {
    switch (action.type) {
        case '1':
            return state
        default:
            return state
    }
}